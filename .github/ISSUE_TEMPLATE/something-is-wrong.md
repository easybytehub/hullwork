---
name: Something is wrong while running it
about: An instance is up and behaving incorrectly. For "I could not get it running", use the other template.
title: ""
labels:
  - bug
---

<!-- Two commands answer most of what would otherwise be a conversation. Paste them and you can leave
     everything else short. -->

```
hullwork --version
```

```
hullwork doctor
```

<!-- `doctor` prints names, never values: no token, no key, no DSN. It is safe to paste whole. If you
     would rather not, say which lines were BROKEN and skip the rest. -->

## What did you expect, and what happened instead?

## What did you run?

<!-- The command, and what came back. If an attempt is involved, the item number is enough — the
     evidence page holds the rest, and pasting it is optional. -->

## How is it deployed?

<!-- One line. `hullwork try` on a laptop, the evaluation stack, or both halves on a host — and
     whether the dispatcher is running. Which forge, tracker and model endpoint, if that is relevant. -->

---

<!-- What not to spend time on: a minimal reproduction, log level tuning, or reading the source first.
     A report that names the symptom and the version is complete. If you already found the cause, say
     so at the top and skip everything above it. -->
