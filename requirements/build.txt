# What the release workflow installs to build a wheel, pinned by hash.
#
# **Scorecard's Pinned-Dependencies, and the reason a bare `pip install build` fails it**: without
# hashes, what gets installed depends on what PyPI serves at that moment — in the one workflow that
# holds a token able to publish packages and create releases. `--require-hashes` makes the resolution
# a fact recorded in this repository instead.
#
# Regenerate deliberately, never by habit:
#     echo build | uv pip compile --generate-hashes - -o requirements/build.txt
#
# The project's own dependencies are **not** pinned this way, and that is a known gap rather than an
# oversight: `uv.lock` exists and does not reproduce a green suite (item 149 measured 38 failures
# against it), so hash-pinning the application install means fixing that first.
# This file was autogenerated by uv via the following command:
#    echo build | uv pip compile --generate-hashes - -o requirements/build.txt
build==1.6.0 \
    --hash=sha256:bd2c8afc603e7a2e0ce70e2ea85f0a6d02043bafbd307f5bada0f98669eca5af \
    --hash=sha256:f7aaf1ebbb79178a02ba248bb524f2176b256017e17e8e4bd4289c7b38cc2bad
    # via the release workflow
packaging==26.3 \
    --hash=sha256:94edc256424af38762eb31306eed28beb9f0efc50a8837492c9d6fd6004aed79 \
    --hash=sha256:d7193f7c8e4e93f444fde0262bf90af30e16fa0ad0ad44cb553c87339b23cd1c
    # via build
pyproject-hooks==1.2.0 \
    --hash=sha256:1e859bd5c40fae9448642dd871adf459e5e2084186e8d2c2a79a824c970da1f8 \
    --hash=sha256:9e5c6bfa8dcc30091c74b0cf803c81fdd29d94f01992a7707bc97babb1141913
    # via build
