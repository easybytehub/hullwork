# The demo branch

Static HTML, published as GitHub Pages. **Rendered by `hullwork.page` against a seeded database** —
the real views with invented data, because a public demo must not carry another project's issue titles.

Regenerated rather than edited: touching these files by hand would make the demo a drawing of the
product instead of the product. See `index.html` for what a visitor is told.
